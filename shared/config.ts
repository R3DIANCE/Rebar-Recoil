export const ROLL_COOLDOWN = 4500;       // Combat rolling spam prevention
export const relativeHeading = 0.12;       // Weapon upwards ratio when not shooting
export const relativePitch = 0.12;         // Weapon left and right ratio when not shooting
export const relativeHeadingNotAiming = 1; //  Weapon upwards ratio when not shooting
export const relativePitchNotAiming = 1;   // Weapon left and right ratio when not shooting
export const shootingLevel = 1000;         // Weapon accuracy

export default { ROLL_COOLDOWN, relativeHeading, relativePitch, relativeHeadingNotAiming, relativePitchNotAiming, shootingLevel };